[project]
name = "metrica-ai-checker"
version = "0.1.0"
description = "Прототип ИИ-агента проверки домашних заданий по математике (платформа МЕТРИКА)"
requires-python = ">=3.11"
dependencies = [
    "fastapi[all]>=0.115.0",
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
serve = "main:main"
